import isWsl from 'is-wsl';
import which from 'which';
import { platform } from 'std-env';
const macPaths = [
    '/usr/local/bin',
    '/opt/homebrew/bin',
    '/opt/local/bin',
    '/usr/bin',
];
const detectPureProgram = async (program) => {
    if (platform !== 'darwin') {
        return true;
    }
    return !!(await which(program, { nothrow: true, path: macPaths.join(':') }));
};
export async function detectProgram(program) {
    if (isWsl) {
        const { x } = await import('tinyexec');
        let windowsPath;
        try {
            windowsPath = (await x('where.exe', [program], { throwOnError: true, nodeOptions: { shell: true } })).stdout;
        }
        catch { /** */ }
        if (windowsPath) {
            return {
                path: windowsPath,
                wsl: false,
                pure: true,
            };
        }
        else {
            let wslPath;
            try {
                wslPath = await which(program);
            }
            catch {
                return null;
            }
            return wslPath
                ? {
                    path: wslPath,
                    wsl: true,
                    pure: true,
                }
                : null;
        }
    }
    else {
        let programPath;
        try {
            programPath = await which(program);
        }
        catch {
            return null;
        }
        return programPath
            ? {
                path: programPath,
                wsl: false,
                pure: await detectPureProgram(program),
            }
            : null;
    }
}
//# sourceMappingURL=npx.js.map